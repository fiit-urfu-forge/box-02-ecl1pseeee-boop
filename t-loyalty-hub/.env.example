# Compose-level overrides. Copy to .env at repo root if you want to customize.
# Keep this in sync with backend/.env.example.

APP_ENV=local
APP_DEBUG=true
APP_KEY=base64:6+eoggJpHY5TiXpdsSBAYO9VxlmsgIOx3EcdNwbJpTY=

# Host port nginx exposes — change if 8080 is in use.
WEB_PORT=8080
