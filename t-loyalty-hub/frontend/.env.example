# Default uses /api on same origin (works behind nginx in Docker).
# Override only when running Vite dev server against a remote backend.
# VITE_API_URL=/api
# VITE_PROXY_TARGET=http://localhost:8080
